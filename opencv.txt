파일 불러오기
cv2.imread
파일 저장하기
cv2.imwrite()
창 생성
cv2.namedWindow()
창에 img 띄우기
cv2.imshow()
대기 함수
cv2.waitkey(delay)
창 하나 종료
cv2.destroyWindow(닫고자 하는 창 이름)
모든 창 종료
cv2.destroyAllWindows()
창 위치 이동
cv2.moveWindow(창 이름,x좌표,y좌표)
창 크기 변경
cv2.resizeWindow(winname,width,height)
영상 출력
cv2.imshow()